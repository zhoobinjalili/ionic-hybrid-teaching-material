import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.hybrid',
  appName: 'ionic-hybrid',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
